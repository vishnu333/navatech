apiVersion: v1
kind: Service
metadata:
  name: {{ include "navatech-app.fullname" . }}
  labels:
    {{- include "navatech-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "navatech-app.selectorLabels" . | nindent 4 }} 